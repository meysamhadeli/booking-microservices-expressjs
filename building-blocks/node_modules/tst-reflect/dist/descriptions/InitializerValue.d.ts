export declare class InitializerValue {
    private readonly _value;
    constructor(_value: Promise<any>);
    /**
     * Returns an initializer value.
     * @desc Returns a Promise because some initializer instantiate classes;
     * in such case it is important to import that class first and then it will be possible to create its instance.
     */
    getValue(): Promise<any>;
}
