import { Type } from "../Type";
import { MetadataStore } from "./MetadataStore";
import { MetadataStoreBase } from "./MetadataStoreBase";
export declare class InlineMetadataStore extends MetadataStoreBase {
    private _store;
    static initiate(): MetadataStore;
    static get(): MetadataStore;
    get store(): {
        [p: number]: Type;
    };
    get(id: number): Type | undefined;
    getLazy(id: number): () => (Type | undefined);
}
